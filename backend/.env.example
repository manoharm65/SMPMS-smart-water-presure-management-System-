# Frontend environment
# If you run the API separately without Vite proxy, set this:
# VITE_API_BASE_URL=http://localhost:8080

# If empty/omitted, the frontend will use relative '/api' (works with Vite proxy)
VITE_API_BASE_URL=
